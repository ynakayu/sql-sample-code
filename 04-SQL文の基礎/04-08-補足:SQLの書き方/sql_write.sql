select id,                    last_name from                          users;

select
    id,
    last_name
from
    users;
    
SELECT ID, LAST_NAME FROM USERS;

SELECT id, last_name FROM users;

SELect ID, LAst_NAme FroM USers;

select id, last_name from users LIMIT 0, 1000;